/* A pretend prototype (not actually needed due to dynamic typing
 * prototype Question {
 *    contructor() {
 *       this.type;
 *       this.subtype;
 *    }
 *    random([n], options) {
 * 	  return new Question ();
 *    }
 *  
 */
